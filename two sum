arr=list(map(int,input().split()))
n=int(input())
found= False

for i in range(len(arr)):
    for j in range(i+1, len(arr)):
        if  arr[i] + arr[j] == n:
            print(arr[i],arr[j])
            found=True
            break

if found is False:
    print("pair not found")
