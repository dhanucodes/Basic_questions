arr=list(map(int,input().split()))
k=int(input())
maximum=0

for i in range(len(arr)):
    total=0
    for j in range(i,len(arr)):
        total+=arr[j]

        if total == k:
            maximum=max(maximum, j-i+1)

print(maximum)
