arr=list(map(int,input().split()))
k=int(input())
count=0

for i in range(len(arr)):
    total=0
    for j in range(i,len(arr)):
        total+=arr[j]

        if total == k:
            count+=1

print(count)
